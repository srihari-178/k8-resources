apiVersion: v1
kind: Pod
metadata:
  name: resources-demo
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        request:
          cpu: "50m"
          memory: "200mi"
        limit:
          cpu: "100m"
          memory: "400mi"
