apiVersion: v1
kind: Namespace
metadata:
  name: roboshop
  labels:
    environment: development
    project: roboshop
spec:
  containers:
    name: nginx
    image: nginx
    ports:
      targetPort: 8080
